{
  "name": "cineos",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node generate-apps.cjs && vite",
    "build": "node generate-apps.cjs && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.2"
  }
}
